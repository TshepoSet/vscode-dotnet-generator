Ignore development source files

src/ .vscode/ .gitignore tsconfig.json pnpm-lock.yaml node_modules/
coverage/

Ignore tests (if added later)

test/ tests/

Ignore local environment files

.env .env.local

Ignore TypeScript sources (only ship compiled JS)

*.ts

Explicitly include required production files

!dist/** !README.md !LICENSE !package.json
